const connection = require('../database/connections');

module.exports = {
        //retornar somente as informações do usuário que está logado
        async index(request, response){
         

        return response.json();
    }
}