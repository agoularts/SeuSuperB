// SUBIR INFOS DOS PRODUTOS

const multer = require('multer')

/*module.exports = {
    storage: multer.diskStorage({
        destination: Path2D.resolve(__dirname, '..', '..', 'uploads'),
        filename: (request, file, callback) => {
            callback(null, `${file.fieldname})
        }
    })
}*/